<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/side_bar_bottom" /> <!-- 배경 색상 설정 -->
    <corners android:radius="10dp" /> <!-- 모서리 반경 설정 -->
    <stroke
        android:width="1dp"
        android:color="@color/profile_border" /> <!-- 테두리 설정 -->
</shape>